{
  "name": "obsidian-claude-code",
  "version": "0.1.0",
  "description": "Run Claude Code directly inside Obsidian. Opens an interactive terminal in a new tab with session persistence across restarts.",
  "main": "main.js",
  "type": "module",
  "private": true,
  "license": "MIT",
  "scripts": {
    "build": "node esbuild.config.mjs",
    "dev": "node esbuild.config.mjs --watch",
    "rebuild-pty": "npx @electron/rebuild -v 39.7.0 -m . -o node-pty",
    "setup": "npm install && npm run rebuild-pty && npm run build",
    "version": "node version-bump.mjs && git add manifest.json versions.json"
  },
  "devDependencies": {
    "@electron/rebuild": "^3.6.0",
    "@types/node": "^20.0.0",
    "esbuild": "^0.20.0",
    "obsidian": "latest",
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "@xterm/addon-fit": "^0.10.0",
    "@xterm/addon-ligatures": "0.10.0",
    "@xterm/xterm": "^5.5.0",
    "node-pty": "^1.0.0"
  }
}
